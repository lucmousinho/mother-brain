MB_TOKEN=
MB_API_PORT=7337
MB_DATA_DIR=./motherbrain
MB_STORAGE_DIR=./storage

# Semantic recall (optional — leave empty for defaults)
MB_EMBEDDING_MODEL=
MB_RECALL_MODE=
MB_VECTOR_PATH=
MB_VECTOR_TOP_K=
MB_MODEL_CACHE_DIR=

# ── OpenClaw integration (used by OpenClawHooks.fromEnv()) ──────────
# All optional — defaults shown in comments.
MB_URL=http://127.0.0.1:7337
# MB_TOKEN is shared with the server (set above)
MB_TIMEOUT_MS=5000
MB_HEALTH_CACHE_MS=30000
MB_ON_UNAVAILABLE=skip
MB_AGENT_ID=openclaw
MB_AGENT_NAME=OpenClaw Agent
MB_CONTEXT_ID=
